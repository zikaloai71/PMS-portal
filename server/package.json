{
  "name": "server",
  "version": "1.0.0",
  "description": "A simple mock API for a Property Management System (PMS), using `json-server`. This simulates listing properties and booking them, perfect for frontend testing and task evaluations.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 4000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
